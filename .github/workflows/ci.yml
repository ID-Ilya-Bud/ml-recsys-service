  name: pipecicd
  on: push
  jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - uses: actions/setup-python@v5
          with:
            python-version: "3.12"
        - name: Install flake8
          run: pip install flake8
        - name: Start flake8
          run: flake8 .
        - name: Build dock-con
          run: docker build -t ml-recsys .